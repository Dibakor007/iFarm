# Frontend (Vite)
# Copy to .env.local and fill values

# Gemini API key (DO NOT COMMIT real keys)
GEMINI_API_KEY=

# Optional: override API base (otherwise Vite proxy /api -> http://localhost:4000)
VITE_API_BASE_URL=
VITE_API_BASE_PATH=
