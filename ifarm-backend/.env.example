# Backend (Express)
# Copy to .env.local (or .env) and fill values

PORT=4000
API_BASE_PATH=/api/v1

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=cold_storage_db

# Gemini API key for backend AI routes (optional)
GEMINI_API_KEY=
